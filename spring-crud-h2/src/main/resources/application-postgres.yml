spring:
  
  datasource:
    url: jdbc:postgresql://localhost:5432/jika-crud
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres
 
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
